# Python virtual environment created by setup.sh
.venv/

# Temporary working files (extracted audio, chunks, per-chunk transcripts)
.work/

# Your videos and results stay on your machine, not in git
input/*
output/*

# but keep the folders themselves
!input/.gitkeep
!output/.gitkeep
